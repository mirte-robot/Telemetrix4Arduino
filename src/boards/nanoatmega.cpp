#include <config.hpp>

#if defined(ARDUINO_ARCH_AVR) && defined(ARDUINO_AVR_NANO)
void hw_init() {
  // No specific hardware initialization needed for Nano
  // This is a placeholder for future use if needed
}

#endif